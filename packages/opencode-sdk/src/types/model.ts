export type ResolvedModelBridge = {
  providerId: string;
  modelId: string;
  variantId: string;
};

export type ResolvedModelBridgeSnakeCase = {
  provider_id: string;
  model_id: string;
  variant_id: string;
};
